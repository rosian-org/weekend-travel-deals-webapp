import Vue from 'vue'
import Vuetify from 'vuetify/lib'
import 'vuetify/src/stylus/app.styl'

Vue.use(Vuetify, {
  iconfont: 'md',
  theme: {
    primary: '#ca125a',
    secondary: '#FFEBEE',
    accent: '#ca125a',
    error: '#ca125a'
  },
})
